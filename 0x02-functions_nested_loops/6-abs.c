#include <stdio.h>
#include "holberton.h"

/**
 * _abs - check the code for ALX School students.
 * @n: int argument for function
 * Return: Always 0.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
return (n * -1);
}
