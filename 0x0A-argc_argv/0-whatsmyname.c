#include "holberton.h"
#include <stdio.h>

/**
 * main - entry point to the programm
 * Description: Prints its name to the std output
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
